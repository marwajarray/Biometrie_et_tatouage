[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tp02-empreinte"
version = "1.0.0"
description = "TP02 - Reconnaissance d’Empreinte Digitale (ORB, FFT, Gabor, SSIM+Contours)"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
